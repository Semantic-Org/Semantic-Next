{
  "name": "@semantic-ui/cdn-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "upload": "node upload.js",
    "deploy-worker": "wrangler deploy",
    "test": "vitest run --config vitest.config.js",
    "test:check": "node check-bare-imports.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  },
  "devDependencies": {
    "@vitest/browser": "^3.1.0",
    "@vitest/browser-playwright": "^0.1.0",
    "vitest": "^3.1.0",
    "wrangler": "^4.0.0"
  }
}
