---
import MobileMenu from '@components/MobileMenu/MobileMenu.js';
import { getSiteMenu } from '@helpers/navigation.js';
import NavMenu from '@components/NavMenu/NavMenu.js';
const siteMenu = await getSiteMenu();
const activeURL = Astro.url.pathname;
---

<MobileMenu
  menu={siteMenu}
  activeURL={activeURL}
  client:load="lit"
/>
