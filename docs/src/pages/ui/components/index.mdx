---
layout: '@layouts/Guide.astro'

---

import { componentPages } from '@helpers/menus.js';
import { Cards, Card } from '@semantic-ui/core';


## Components

<Cards three>
  {(componentPages || []).map((page) => (
    <Card
        header={page.name}
        href={page.url}
        description={page.description}
      ></Card>
  ))}
</Cards>
