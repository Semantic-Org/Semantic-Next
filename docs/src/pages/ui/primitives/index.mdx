---
layout: '@layouts/Guide.astro'

---

import { primitivePages } from '@helpers/menus.js';
import { UICards, UICard } from '@semantic-ui/core';

## Primitives

<UICards three>
  {(primitivePages || []).map((page) => (
    <UICard
        href={page.url}
      >
        <div slot="header">{page.name}</div>
        <div slot="description"><img src="/components/input.gif" /></div>
      </UICard>
  ))}
</UICards>
