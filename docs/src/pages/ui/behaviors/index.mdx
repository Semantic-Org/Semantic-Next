---
layout: '@layouts/Guide.astro'
icon: cpu
---

import { behaviorPages } from '@helpers/menus.js';
import { Cards, Card } from '@semantic-ui/core';


## Behaviors

<Cards three>
  {(behaviorPages || []).map((page) => (
    <Card
        header={page.name}
        href={page.url}
        description={page.description}
      ></Card>
  ))}
</Cards>
