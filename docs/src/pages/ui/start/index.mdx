---
layout: '@layouts/Guide.astro'
icon: open book
title: Introduction
description: An introduction to Semantic UI
---
import { UICards, UICard } from '@semantic-ui/core';
import { Tabs, TabItem } from '@astrojs/starlight/components';
import PlaygroundExample from '@components/PlaygroundExample/PlaygroundExample.astro';
import Blockquote from '@components/mdx/Blockquote.astro';

export const components = { blockquote: Blockquote };

## Using Semantic UI

Semantic UI is a UI framework built with web components that can be used anywhere you write html.

### As a Designer / Developer

Semantic gives you access to a [UI framework](/ui/start/guides/using-ui) with components like `button`, `dropdown` and `menu` for building a website.

1) Choose from [primitives](/ui/primitives), [components](/ui/components), and [behaviors](/ui/behaviors) you need for your website.
2) Include them wherever you write html as html tags like [`<ui-button>`](/ui/primitives/button).
3) [Theme your components](/ui/start/guides/theming) using standard css and css variables.

### As an Organization

Semantic provides an [constellation of tools](/docs/guides) for authoring web components that let you use UI across multiple ecosystems like mobile devices, javascript frameworks like React, Vue, or Svelte, as well as anywhere else where html is written.

1) Create custom organization specific web components like `<order-form>` built with ui primitives like `<ui-button>`, `<ui-input>` etc.
2) Publish your custom UI components as part of a design system for your company.
3) Use your custom html tags wherever your company ships html.

> [!info] Why Semantic
> Semantic ships with modern features like **Signals based reactivity**, a custom **AST-based templating language**, and can be **run natively in the browser without a build step** unlike most other large frameworks. [Learn more](/ui/start/why-semantic)


## Where to Start

* [Install Semantic UI](/start/install) in your project and play around with it.
* Check out the [TodoMVC example](/examples/todo-list) to see how to build complex stateful components.
* Explore [API docs](/docs/guides) for standards-based baseline 2026 libraries like the shadow dom aware [Query library](/docs/guides/query)
