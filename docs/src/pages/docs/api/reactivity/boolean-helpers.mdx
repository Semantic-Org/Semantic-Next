---
layout: '@layouts/Guide.astro'
pageType: 'API Reference'
title: Boolean Helpers
icon: toggle-left
package: "@semantic-ui/reactivity"
methods: [toggle]
description: API reference for Boolean Helpers in Semantic UI's reactivity system
---
import PlaygroundExample from '@components/PlaygroundExample/PlaygroundExample.astro';
import Blockquote from '@components/mdx/Blockquote.astro';

export const components = { blockquote: Blockquote };

Signal helper for boolean values. Flips the stored value in place without a read-mutate-write round trip.

## Toggling

### toggle

```javascript
signal.toggle();
```

Sets the value to its boolean negation, notifying dependents when it changes. Equivalent to `signal.mutate(value => !value)`.

#### Usage

```javascript
import { signal } from '@semantic-ui/reactivity';

const isOpen = signal(false);
isOpen.toggle(); // true
isOpen.toggle(); // false
```

> [!note] Coerces non-booleans
> `toggle()` negates whatever is stored, so a truthy non-boolean becomes `false` and `undefined` becomes `true`. Start from a boolean to keep the value boolean.

#### Example

<PlaygroundExample id="reactive-toggle" direction="horizontal"></PlaygroundExample>
