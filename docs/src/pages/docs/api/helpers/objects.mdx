---
layout: '@layouts/Guide.astro'
pageType: 'API Reference'
title: Object Helpers
icon: box
description: API reference for object manipulation template helpers
package: '@semantic-ui/templating'
methods: ['arrayFromObject']
---

Object helpers provide utilities for working with objects in templates.

## Functions

### arrayFromObject
Converts an object to an array for use with `{#each}`.

#### Template Syntax
```sui
{#each arrayFromObject objectToConvert}
  {key}: {value}
{/each}
```

#### Parameters
| Name | Type   | Description |
|------|--------|-------------|
| obj  | object | The object to convert |

#### Returns
`array` - An array of key-value pair objects.

#### Example
```sui
<dl>
  {#each arrayFromObject user}
    <dt>{key}</dt>
    <dd>{value}</dd>
  {/each}
</dl>
```

