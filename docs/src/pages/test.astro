---
import Body from '../layouts/Body.astro';
import TopBar from '@components/TopBar.astro';

import TopbarMenu from '@components/TopbarMenu/TopbarMenu.js';
import {UIButton} from '@semantic-ui/core';
const menu = [];
---
<Body padded>
  <UIButton client:only class="disabled one">Test</UIButton>
  <UIButton client:only class="toggle">Toggle</UIButton>
  <script>
    import { $ } from '@semantic-ui/query';
    $('.toggle').on('click', () => {
      $('.one').toggleClass('disabled');
    });
  </script>

</Body>
