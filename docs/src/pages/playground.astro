---
  import Playground from '@layouts/Playground.astro';
  import {
    htmlBefore,
    htmlAfter,
    cssBefore,
    jsBefore,
    importMap
  } from '@helpers/playground.js';

  const htmlContent = `<ui-button primary>Button</ui-button>`;
  const cssContent = `ui-button {
  font-size: 18px;
}`;
  const jsContent = `document.body.appendChild(document.createTextNode("World!"))`;

  const projectConfig = {
    importMap: importMap,
    files: {
      'index.js': {
        contentType: 'text/javascript',
        content: `${jsBefore}${jsContent}`
      },
      'index.html': {
        contentType: 'text/html',
        content: `${htmlBefore}${htmlContent}${htmlAfter}`
      },
      'index.css': {
        contentType: 'text/css',
        content: `${cssBefore}${cssContent}`
      }
    }
  };
---
<script>
  import 'playground-elements/playground-ide.js';
  import 'playground-elements/playground-project.js';
  import 'playground-elements/playground-file-editor.js';
  import 'playground-elements/playground-preview.js';
</script>
<Playground>
  <playground-ide id="source" sandboxBaseUrl="https://semantic-next.vercel.app/"></playground-ide>
  <script define:vars={{ projectConfig }}>
    document.querySelector('playground-ide').config = projectConfig;
  </script>
  <script>
    const editor = document.querySelectorAll('playground-file-editor');
  </script>
</Playground>
