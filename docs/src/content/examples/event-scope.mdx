---
title: 'Event Scope'
exampleType: 'component'
category: 'Framework'
subcategory: 'Events'
tags: ['component', 'events', 'scope', 'each', 'getting-started']
tip: '`scope` resolves the template vars acting at the event target — each item vars, subtemplate args, snippet args — so list handlers never round-trip identity through `data-*` attributes'
description: Reading each item vars from event handlers with scope
---
