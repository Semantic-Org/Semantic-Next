---
title: 'trackWrites'
id: 'utils-trackwrites'
exampleType: 'log'
category: 'Utils'
subcategory: 'Objects'
description: 'Run a callback against a value and detect whether it changed it'
tags: ['utils', 'objects', 'trackWrites', 'change detection', 'mutation', 'proxy']
selectedFile: 'index.js'
tip: 'Small values are snapshot-compared so the callback sees the real object, large values get a write-tracking wrapper that shows as Proxy(Object) in the console — pass strategy to pin either path'
---
