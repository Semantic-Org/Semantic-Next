---
title: 'parseId'
id: 'utils-parseid'
sortIndex: 2
exampleType: 'log'
category: 'Utils'
subcategory: 'Crypto'
description: 'Split an id into its parts and decode its timestamp'
tags: ['utils', 'crypto', 'parseid', 'ulid', 'timestamp', 'id']
selectedFile: 'index.js'
tip: 'A db id is a ULID, so its creation time is encoded in the id itself — parseId hands it back as a Date with no extra column. Returns null for anything isValidId would reject'
---
