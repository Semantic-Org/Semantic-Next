---
title: 'generateId'
id: 'utils-generateid'
sortIndex: 0
exampleType: 'log'
category: 'Utils'
subcategory: 'Crypto'
description: 'Generate unique ids with usage presets, prefixes, and checksums'
tags: ['utils', 'crypto', 'generateid', 'ulid', 'uuid', 'id', 'random', 'secret', 'entropy']
selectedFile: 'index.js'
tip: 'The usage preset encodes the consensus length and shape per channel so you do not have to reason about collision budgets — db is a sortable ULID, page is letter-first for CSS ids, link is URL-sized, token carries a checksum, secret is a 256-bit credential, code is a grouped uppercase human-typed code. Pair with isValidId and parseId'
---
