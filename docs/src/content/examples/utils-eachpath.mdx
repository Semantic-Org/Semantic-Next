---
title: 'eachPath'
id: 'utils-eachpath'
sortIndex: 5
exampleType: 'log'
category: 'Utils'
subcategory: 'Paths'
description: 'Walk every path a path string passes through, shortest first'
tags: ['utils', 'objects', 'eachPath', 'path', 'ancestors', 'iterate', 'walk']
selectedFile: 'index.js'
tip: 'Pass self: false to visit only the ancestors above the target — the walk a subsumption check needs, since a write logged at any ancestor of a path covers that path. Each visited value is itself resolvable through get, and returning false from the callback stops the walk early, like each'
---
