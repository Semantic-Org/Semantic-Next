---
title: 'toBytes'
id: 'utils-tobytes'
sortIndex: 7
exampleType: 'log'
category: 'Utils'
subcategory: 'Coercion'
description: 'Coerce a string, buffer, typed array, or array of byte values to a Uint8Array, or null when there are no bytes'
tags: ['utils', 'coercion', 'tobytes', 'bytes', 'uint8array', 'binary', 'buffer', 'utf-8']
selectedFile: 'index.js'
tip: 'A string reads as its UTF-8 text, never as an encoding. To get bytes out of base64 use fromBase64 with as: bytes. Typed arrays and buffers become a view over the same memory, no copy'
---
