---
title: 'stringifyCSS'
id: 'utils-stringifycss'
exampleType: 'log'
category: 'Utils'
subcategory: 'CSS'
description: 'Write a tree of rules back to CSS text in one canonical layout'
tags: ['utils', 'css', 'stringifycss', 'serialize', 'server']
selectedFile: 'index.js'
tip: 'parseCSS(stringifyCSS(nodes)) reads back as the same tree, so a sheet survives a round trip'
---
