---
title: 'debounce (async)'
id: 'utils-debounce-async'
sortIndex: 6
exampleType: 'log'
category: 'Utils'
subcategory: 'Functions'
description: 'Calls share one promise, and cancel() rejects it for everyone awaiting'
tags: ['utils', 'functions', 'debounce', 'async', 'promise', 'cancel', 'pending']
selectedFile: 'index.js'
tip: '`cancel()` rejects the shared promise with an Error whose `code` is `CANCELLED`, so an `await` on a debounced call needs a catch'
---