---
title: 'formatDate'
id: 'utils-formatdate'
exampleType: 'log'
category: 'Utils'
subcategory: 'Dates'
description: 'Token strings, named presets, and per-call locale and timezone'
tags: ['utils', 'dates', 'formatdate', 'i18n', 'presets', 'timezone']
selectedFile: 'index.js'
tip: "The `{formatDate}` template helper defaults to format `'L'` and `timezone: 'local'`, the import to `'LLL'` and UTC, so moving a call between them can change both the shape and the clock"
---