import { tokenize } from '@semantic-ui/utils';

console.log(tokenize('Hello World'));
console.log(tokenize('My Component Name'));
console.log(tokenize('Special@#$Characters'));
console.log(tokenize('under_score and spaces'));
console.log(tokenize('UPPERCASE Text'));
