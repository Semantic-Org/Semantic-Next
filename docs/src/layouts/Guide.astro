---
import Layout from '@layouts/Layout.astro';
import './Guide.css';
import { getRailMenu } from '@helpers/navigation.js';

const { frontmatter } = Astro.props;

const railMenu = getRailMenu(Astro.props.headings, frontmatter.title);
const searchMeta = {
	title: frontmatter.title,
	description: frontmatter.description,
	icon: frontmatter.icon,
	pageType: frontmatter.pageType || 'Guide'
}
---
<Layout
	inlineHeader=true
	header={frontmatter.title}
	headerIcon={frontmatter.icon}
	headerImage={frontmatter.iconImage}
	searchMeta={searchMeta}
	title={searchMeta.title}
	textContent=true
	expandRailMenu=true
	railMenuScrollOffset=-20
	railMenu={railMenu}
>
	<slot />
</Layout>
