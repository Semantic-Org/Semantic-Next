- Utility library that parses spec file
-- Should be able to generate code samples in various formats (html, astro, react, vue) as strings

getDefinition({ type: 'singular', minUsageLevel: 3 })
getDefinition({ type: 'plural', minUsageLevel: 1 })

// returns menu for rail
getDefinitionMenu({ type: 'plural' })

// returns component from exports
getComponent()
getComponent({ type: 'plural '})

// returns the actual sample code
getSampleCode('emphasis', { language: 'astro' })

getExample('allowAdditions');

Component Definition

--



Code Component

Docs layout
- Top menu for docs page
- Following menu
- Code component

Button
- Finish button group

Docs
- Rewrite readmes as docs

Components
- Figure out build step for component spec

